# HTML5 Forms и валидация

## Вопросы

1. Как работает нативная HTML5-валидация форм? Какие основные атрибуты и псевдоклассы используются?

2. Какие недостатки у нативной валидации? В каких случаях ты предпочитаешь кастомную?

3. Как сделать кастомную валидацию доступной для экранных читалок? (ARIA-live, aria-errormessage, aria-invalid и т.д.)
  Приведи пример формы с email + password + confirm password.



## Нативная валидация

Нативная HTML5-валидация форм — это встроенная в браузер система проверки корректности данных, введённых пользователем, без JavaScript.
Она работает автоматически при отправке формы и предоставляет визуальную обратную связь (всплывающие подсказки) и API для кастомизации.


### 🔧 Как это работает?

1. При отправке формы (form с кнопкой type="submit") браузер:
  * Проверяет все поля на соответствие заданным правилам.
  * Если есть ошибки — останавливает отправку, показывает всплывающее сообщение у первого невалидного поля.
  * Добавляет CSS-псевдоклассы (:invalid, :valid) для стилизации.

2. Валидация происходит "лениво":
  * Пока поле не было затронуто (:user-invalid), ошибки не показываются.
  * После первого взаимодействия — проверка активируется.

### 📌 Основные атрибуты валидации

1. required

Поле обязательно для заполнения

```html
<input type="text" required>
```

2. min, max, step (для чисел и дат)

Ограничения по значению.

```html
<input type="number" min="0" max="100" step="5">
<input type="date" min="2025-01-01" max="2026-12-31">
```

3. minLength, maxLength

Длина текста.

```html
<input type="text" minlength="1" maxlength="60">
<textarea minlength="10"></textarea>
```

4. pattern

Регулярное выражение для text, email, tel, password и др.

```html
<input type="text" pattern="[A-Za-z]{2,20}" title="Только буквы, 2–20 символов">
```

5. Специальные типы полей (автоматическая валидация)
  * type="email" -> проверка формата email
  * type="url" -> проверка URL
  * type="number" -> только числа
  * type="tel" -> нет строгой валидации, но полезен для мобильных клавиатур

```html
<input type="email" required> <!-- Должен содержать @ и домен -->
```

6. novalidate (на форме)

Отключает валидацию.

```html
<form novalidate>
  <!-- Валидация отключена -->
</form>
```

### 🎨 Псевдоклассы CSS для стилизации


| Псевдокласс       | Когда применяется                                      |
|-------------------|--------------------------------------------------------|
| **:valid**        | Поле прошло валидацию                                  |
| **:invalid**      | Поле не прошло валидацию                               |
| **:required**     | Поле имеет атрибут required                            |
| **:optional**     | Поле не имеет атрибут required                         |
| **:user-valid**   | Поле валидно и пользователь уже взаимодействовал с ним |
| **:user-invalid** | Поле невалидно и пользователь уже взаимодействовал     |

>  :user-valid / :user-invalid — экспериментальные, но уже поддерживаются в Chrome, Edge, Firefox.

Пример стилизации:

```css
input:invalid {
  border: 2px solid red;
}

input:valid {
  border: 2px solid green;
}

/* Показывать ошибку только после взаимодействия */
input:user-invalid {
  box-shadow: 0 0 5px rgba(255, 0, 0, 0.5);
}
```

### 🛠️ JavaScript API (опционально)

Хотя валидация работает без JS, можно управлять ею программно:

```js
const input = document.querySelector('#email');

// Проверить валидность
console.log(input.validity.valid); // true/false

// Показать/скрыть сообщение об ошибке
input.reportValidity(); // как при submit
input.setCustomValidity('Пользовательское сообщение');
input.setCustomValidity(''); // сбросить

// Слушать изменения
input.addEventListener('invalid', (e) => {
  e.preventDefault(); // отменить стандартное сообщение
  // свой UI ошибки
});
```

Объект validity содержит детали:

```js
input.validity.valueMissing;    // true, если required, но пусто
input.validity.typeMismatch;    // true, если email/url неверный
input.validity.patternMismatch; // true, если не совпадает pattern
// и др.
```

### ✅ Преимущества нативной валидации

* Работает без JavaScript (деградация gracefully).
* Автоматическая локализация сообщений (на языке ОС/браузера).
* Интеграция с экранными дикторами (доступность!).
* Меньше кода -> меньше багов.


### ⚠️ Ограничения

* Нельзя полностью кастомизировать внешний вид всплывающих подсказок (но можно отключить через novalidate и сделать свой UI).
* Сложные правила (например, «пароль и подтверждение должны совпадать») требуют JavaScript.
* Поведение может немного отличаться между браузерами.

### Пример простой формы

```html
<form>
  <label>
    Email:
    <input type="email" required title="Введите корректный email">
  </label>
  
  <label>
    Возраст:
    <input type="number" min="18" max="100" required>
  </label>
  
  <button type="submit">Отправить</button>
</form>

<style>
  input:user-invalid {
    border-color: #d32f2f;
  }
  input:user-valid {
    border-color: #388e3c;
  }
</style>
```

Такой подход обеспечивает доступность, простоту и надёжность — основу хорошего UX.

## ARIA

Чтобы кастомная валидация формы была доступной для пользователей экранных читалок (скринридеров),
нужно правильно использовать ARIA-атрибуты и управлять объявлением ошибок через aria-live.
Ниже — пошаговое руководство и полный пример формы с email, паролем и подтверждением пароля.


### ✅ Ключевые ARIA-атрибуты для доступной валидации

| Атрибут                                 | Назначение                                              |
|-----------------------------------------|---------------------------------------------------------|
| **aria-invalid="true"**                 | Указывает, что поле содержит ошибку.                    |
| **aria-errormessage="ID_сообщения"**    | Связывает поле с элементом, содержащим текст ошибки.    |
| **role="alert" или aria-live="polite"** | Объявляет динамически появляющееся сообщение об ошибке. |
| **aria-describedby (альтернатива)**     | Может использоваться для описания поля, включая ошибки. |

> ⚠️ Важно:
> * Сообщение об ошибке должно существовать в DOM до его отображения (можно скрыть через hidden или display: none).
> * Используйте aria-live="polite" для несрочных уведомлений (например, при потере фокуса).
> * Не используйте role="alert" слишком часто — он прерывает текущую речь скринридера.


### 🧩 Пример: форма с email + password + confirm password

```html
<form id="signupForm" novalidate>
  <!-- Email -->
  <div class="field">
    <label for="email">Email</label>
    <input
      type="email"
      id="email"
      name="email"
      aria-invalid="false"
      aria-errormessage="email-error"
    >
    <div id="email-error" role="alert" aria-live="polite" class="error hidden"></div>
  </div>

  <!-- Password -->
  <div class="field">
    <label for="password">Пароль</label>
    <input
      type="password"
      id="password"
      name="password"
      aria-invalid="false"
      aria-errormessage="password-error"
    >
    <div id="password-error" role="alert" aria-live="polite" class="error hidden"></div>
  </div>

  <!-- Confirm Password -->
  <div class="field">
    <label for="confirmPassword">Подтвердите пароль</label>
    <input
      type="password"
      id="confirmPassword"
      name="confirmPassword"
      aria-invalid="false"
      aria-errormessage="confirm-error"
    >
    <div id="confirm-error" role="alert" aria-live="polite" class="error hidden"></div>
  </div>

  <button type="submit">Зарегистрироваться</button>
</form>
```

```css
.error {
  color: #d32f2f;
  margin-top: 4px;
}

.hidden {
  display: none;
}
```

```js
const form = document.getElementById('signupForm');

// Вспомогательная функция для установки ошибки
function setFieldError(fieldId, message) {
  const field = document.getElementById(fieldId);
  const errorEl = document.getElementById(`${fieldId}-error`);

  if (message) {
    field.setAttribute('aria-invalid', 'true');
    errorEl.textContent = message;
    errorEl.classList.remove('hidden');
  } else {
    field.setAttribute('aria-invalid', 'false');
    errorEl.textContent = '';
    errorEl.classList.add('hidden');
  }
}

// Валидация email
function validateEmail(email) {
  if (!email) return 'Email обязателен';
  if (!/^\S+@\S+\.\S+$/.test(email)) return 'Некорректный email';
  return null;
}

// Валидация пароля
function validatePassword(pwd) {
  if (!pwd) return 'Пароль обязателен';
  if (pwd.length < 8) return 'Пароль должен быть не короче 8 символов';
  return null;
}

// Проверка совпадения паролей
function validateConfirmPassword(pwd, confirm) {
  if (!confirm) return 'Подтверждение пароля обязательно';
  if (pwd !== confirm) return 'Пароли не совпадают';
  return null;
}

// Обработчик на форму
form.addEventListener('submit', (e) => {
  e.preventDefault();

  const email = form.email.value.trim();
  const password = form.password.value;
  const confirmPassword = form.confirmPassword.value;

  const emailError = validateEmail(email);
  const pwdError = validatePassword(password);
  const confirmError = validateConfirmPassword(password, confirmPassword);

  setFieldError('email', emailError);
  setFieldError('password', pwdError);
  setFieldError('confirmPassword', confirmError);

  // Если все валидно — отправляем
  if (!emailError && !pwdError && !confirmError) {
    alert('Форма валидна! Отправка...');
    // здесь — fetch или другая логика
  }
});

// Опционально: валидация при потере фокуса (blur)
['email', 'password', 'confirmPassword'].forEach(id => {
  document.getElementById(id).addEventListener('blur', () => {
    const value = form[id].value;
    let error = null;

    if (id === 'email') error = validateEmail(value);
    else if (id === 'password') error = validatePassword(value);
    else if (id === 'confirmPassword') error = validateConfirmPassword(form.password.value, value);

    setFieldError(id, error);
  });
});
```
