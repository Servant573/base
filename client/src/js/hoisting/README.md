# Hoisting

1. Что поднимается (hoists) в JS: var, let/const, function declarations, class?
2. Приведи пример кода, где из-за TDZ возникнет ошибка ReferenceError.


Hoisting - это процесс доступа к переменным до их определения.
Компиляция кода js проходит в 2 подхода. В первом js получает все объявления переменных, все идентификаторы.
Пока никакой код не исполняется.
Исполнение кода происходит на при втором подходе.

То есть как будто происходит поднятие кода с определением переменных и функций вверх до их непосредственного использования.
Поднятие на английский переводится как hoisting.

## Переменные var

var переменные попадают под hoisting. Им присваивается значение undefined

```js
console.log(a) // Здесь будет ошибка ReferenceError: a is not defined
```

```js
console.log(a);   // undefined Компилятор уже знает о переменной a, но пока она не определена
var a = "Test";
console.log(a); // "Test"
```


## const, let

С переменными const, let всё строже. Попытка вызвать их до объявления вызовет ошибку "Uncaught ReferenceError: Cannot access 'foo' before initialization"
На этом всё.


## Функции

Здесь играет роль как мы объявляем функцию.

```js
const name = getName() // Нет ошибки. Функцию объявили через function

function getName() {
  return 'Vitya'
}
```

```js
const name = getName() // Ошибка Uncaught ReferenceError: getName is not defined. Функцию объявили через переменную.

const getName = function() {
  return 'Vitya'
}
```

## Классы

Классы тоже поднимаются, однако не инициализируются как функции

```js
const myInstance = new MyClass(); // ReferenceError: Cannot access 'MyClass' before initialization

class MyClass {
  constructor() {
    console.log("Class instantiated");
  }
}
```

## Temporal Dead Zone (TDZ)

Например, ошибки TDZ возникают при работе с асинхронными операциями, когда переменная объявляется позже вызова. 
Понимание областей видимости помогает избежать багов в больших проектах, где одни и те же имена переменных могут использоваться в разных модулях.

Пример:

```js
function doSome() {
    if (true) {
        console.log(x, y); // 🚨 Ошибка!
    }

    let x = 2;
    var y = 3;

    console.log(x, y); // Этот код не выполнится
  
  /*
  *
Что происходит:

let x:

Объявлена после блока if, но попытка чтения происходит до объявления.

Из-за TDZ обращение к x до инициализации вызовет ошибку: ReferenceError: Cannot access 'x' before initialization.

var y:

Поднимается в начало функции со значением undefined, поэтому доступна (но выведет undefined без ошибки).

Итог: Код упадёт на первом console.log, так как x находится в TDZ.
  * */

}
```


## Источники
[metanit.com](https://metanit.com/web/javascript/3.6.php)
[habr.com](https://habr.com/ru/companies/bitrix/articles/912910/)
